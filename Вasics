~ Шапkа
#include<stdio.h> // подключение библиотек
#include<math.h> // math library

int main(){  // точка входа в программу
}

~ Переменные
int x = 2; // объявление переменной
• s+=n <=> s = s + n
• n-=1 <=> n = n - 1
• x=--n // меняется значение x ; меняется значение n
• n-- // не меняется значение х ; меняется значение n
~ Пример
x=--n, n==10
// х == 9, n == 9
x=n--, n==10
// х == 10, n == 9

int - целочисленный тип [4 байта]
char - символ [1 байт]
float - дроби [4 байта] 
double - дроби с двойной точностью [8 байт]

~ Ввод и вывод
1) Вывод
printf("x is %d and y is %d", x, y);
2) Ввод 
scanf("%d %d", &x, &y);

~ Oперации
• 7 / 4 = 1 (1.21231)
• 7 % 2 = 1 (3*2+1)
• == - равенство, то есть 2==2 (но не 2=2)
• != - не равноо, то есть 2!=3 
• 0 - ложь
• 1 - истина
• && - и (би)
• || - или (би)
• ! - не (унарная)

• {} - блок

~ Математика
#include<math.h>
• fabs(x) модуль числа x
• sqrt(x) квадратный корень из числа x
• sin(x) синус числа x (х в радианах)
• cos(x) косинус числа x (х в радианах)
• pow(x, y) вычисление x^y
• exp(x) вычисление e^x
• log(x) натуральный логарифм числа x
• log10(x) десятичный логарифм числа x
• M_PI — число π



































